<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.maryamabdulrhman.ais">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".SignupDoctor">  </activity>
        <activity android:name=".signUpPatient">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter></activity>


        <activity android:name=".MainActivityPatient" />
        <activity android:name=".MainDoctorActivity" />
        <activity android:name=".ForgetpassworedActivity" />
        <activity android:name=".ReportCoronaActivity"></activity>
    </application>

</manifest>